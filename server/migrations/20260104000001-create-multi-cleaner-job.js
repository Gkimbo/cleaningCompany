"use strict";

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  up: async (queryInterface, Sequelize) => {
    await queryInterface.createTable("MultiCleanerJobs", {
      id: {
        type: Sequelize.INTEGER,
        allowNull: false,
        autoIncrement: true,
        primaryKey: true,
      },
      appointmentId: {
        type: Sequelize.INTEGER,
        allowNull: false,
        references: {
          model: "UserAppointments",
          key: "id",
        },
        onDelete: "CASCADE",
      },
      totalCleanersRequired: {
        type: Sequelize.INTEGER,
        allowNull: false,
        defaultValue: 2,
      },
      cleanersConfirmed: {
        type: Sequelize.INTEGER,
        allowNull: false,
        defaultValue: 0,
      },
      status: {
        type: Sequelize.ENUM(
          "open",
          "partially_filled",
          "filled",
          "in_progress",
          "completed",
          "cancelled"
        ),
        allowNull: false,
        defaultValue: "open",
      },
      primaryCleanerId: {
        type: Sequelize.INTEGER,
        allowNull: true,
        references: {
          model: "Users",
          key: "id",
        },
        onDelete: "SET NULL",
      },
      isAutoGenerated: {
        type: Sequelize.BOOLEAN,
        allowNull: false,
        defaultValue: false,
        comment: "True if system flagged as large home, false if cleaner chose multi-cleaner",
      },
      totalEstimatedMinutes: {
        type: Sequelize.INTEGER,
        allowNull: true,
        comment: "Total estimated minutes to complete the job",
      },
      openedToMarketAt: {
        type: Sequelize.DATE,
        allowNull: true,
        comment: "When the job was opened to market for additional cleaners",
      },
      filledAt: {
        type: Sequelize.DATE,
        allowNull: true,
        comment: "When all cleaner slots were filled",
      },
      urgentNotificationSentAt: {
        type: Sequelize.DATE,
        allowNull: true,
        comment: "When 7-day urgent notification was sent",
      },
      finalWarningAt: {
        type: Sequelize.DATE,
        allowNull: true,
        comment: "When 3-day final warning was sent to homeowner",
      },
      createdAt: {
        type: Sequelize.DATE,
        allowNull: false,
        defaultValue: Sequelize.literal("CURRENT_TIMESTAMP"),
      },
      updatedAt: {
        type: Sequelize.DATE,
        allowNull: false,
        defaultValue: Sequelize.literal("CURRENT_TIMESTAMP"),
      },
    });

    // Index for finding open jobs
    await queryInterface.addIndex("MultiCleanerJobs", ["status"]);
    await queryInterface.addIndex("MultiCleanerJobs", ["appointmentId"]);
  },

  down: async (queryInterface, Sequelize) => {
    await queryInterface.dropTable("MultiCleanerJobs");
  },
};
